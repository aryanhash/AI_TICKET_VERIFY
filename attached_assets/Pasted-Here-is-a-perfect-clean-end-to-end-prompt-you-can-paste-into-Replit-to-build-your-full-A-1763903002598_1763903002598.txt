Here is a **perfect, clean, end-to-end prompt** you can paste into **Replit** to build your full **AI-Verified NFT Ticketing System** using:

* **Frontend:** TypeScript (Next.js or Pure TS React — Replit will choose automatically)
* **Backend:** **Python FastAPI**
* **Blockchain:** **QIE Blockchain SDK + QIEDEX Token Creator**
* **Smart Contract:** NFT Ticket Contract (ERC721-like)
* **AI:** Vision Model for selfie-to-NFT comparison
* **DB:** MongoDB
* **Auth:** Wallet-based login (MetaMask + QIE network)

This prompt includes *all QIE functionality*, all SDK usage, and all required hackathon features.

---

# 🚀 **READY-TO-PASTE PROMPT FOR REPLIT**

> **Build a complete full-stack project called “AI-Verified NFT Ticketing System” using the following exact technologies and features:**
>
> ---
>
> ## **🔧 Technology Requirements**
>
> * **Frontend:** TypeScript (use React + TS or Next.js with TypeScript)
> * **Backend:** Python **FastAPI**
> * **Database:** MongoDB
> * **Blockchain:** QIE Blockchain
>
>   * Use **QIE Blockchain SDK** for:
>
>     * wallet signature verification
>     * reading NFT ownership
>     * minting NFT tickets
>   * Use **QIEDEX Token Creator** for deploying the NFT contract
> * **Smart Contract:** ERC721-like NFT Ticket Contract (use the contract address + ABI generated from QIEDEX Token Creator)
> * **AI:** Vision LLM for selfie-to-NFT image similarity verification
>
> ---
>
> ## **🎯 Core Features to Implement**
>
> ### **User Features**
>
> * Connect MetaMask wallet with QIE Testnet/Mainnet support
> * Wallet-signature login (no passwords)
> * Browse all events
> * Buy NFT-based tickets
> * Ticket purchase → call backend → backend uses QIE SDK to **mint ticket NFT**
> * View wallet-owned tickets (fetched via blockchain)
> * At event entry → scan QR code → upload selfie → AI verifies face against NFT metadata image
> * Show “Verified / Suspicious / Denied”
>
> ### **Organizer Features**
>
> * Organizer wallet login
> * Create events (title, description, date, venue, image, ticket price, ticket supply)
> * Upload base ticket image → store on IPFS
> * Mint tickets in bulk or dynamically
> * View sold tickets
> * View verification logs
>
> ---
>
> ## **📁 Backend (FastAPI) Requirements**
>
> Create the following structure:
>
> ```
> /server
>   main.py
>   database.py
>   /routes
>       auth.py
>       events.py
>       tickets.py
>       verify.py
>   /models
>       user.py
>       event.py
>       ticket.py
>   /services
>       blockchain.py       # QIE SDK usage for minting, signature verification
>       ipfs_service.py     # Upload metadata & images to IPFS
>       ai_verify.py        # Vision LLM selfie comparison
> ```
>
> ### **Backend Functionality**
>
> Implement these endpoints:
>
> ```
> POST /auth/wallet        → verify wallet signature using QIE SDK
> POST /events             → create event (organizer only)
> GET /events              → list events
> GET /events/{eventId}    → event details
> POST /tickets/mint       → mint NFT ticket using QIE SDK
> GET /tickets/{wallet}    → get wallet-owned tickets
> POST /verify             → selfie vs NFT metadata image comparison
> ```
>
> ### **QIE Blockchain Integration**
>
> In `blockchain.py`, create:
>
> * Function to initialize QIEWeb3 provider
> * Function to load deployed NFT contract (address + ABI from QIEDEX Token Creator)
> * `mint_ticket(wallet_address, metadata_uri)`
> * `verify_signature(message, signature, wallet)`
> * `get_tickets_of_owner(wallet)`
>
> Use QIE SDK everywhere transactions or contract calls are needed.
>
> ---
>
> ## **🎨 Frontend Requirements (TypeScript)**
>
> Use React/Next.js + TypeScript with TailwindCSS.
>
> ### Pages:
>
> ```
> /pages (or /src/pages)
>   index.tsx             → Home
>   events.tsx            → events listing
>   events/[id].tsx       → Event details + buy ticket
>   dashboard.tsx         → user tickets
>   verify.tsx            → QR scan + selfie upload
>   organizer/create.tsx  → event creation
> ```
>
> ### Components:
>
> ```
> Navbar.tsx
> EventCard.tsx
> TicketCard.tsx
> QRScanner.tsx
> ```
>
> ### Frontend Functionality:
>
> * Use **ethers.js + TypeScript** to connect wallet
> * Detect QIE Network (mainnet/testnet RPC)
> * Wallet-signature login → send to FastAPI
> * Call `/tickets/mint` API after payment flow
> * Display QR codes for each NFT ticket
> * Verification page:
>
>   * Scan QR
>   * Upload selfie
>   * Send to `/verify`
>   * Display result
>
> ---
>
> ## **🤖 AI Verification Service**
>
> In `ai_verify.py`:
>
> * Fetch NFT metadata from IPFS
> * Extract ticket image
> * Compare selfie (uploaded by user) vs ticket image using Vision LLM
> * Return `"verified" | "face mismatch" | "low similarity"`
>
> ---
>
> ## **🧱 Smart Contract Requirements**
>
> Use **QIEDEX Token Creator** to deploy the NFT Ticket Contract.
> Then import:
>
> * `CONTRACT_ADDRESS`
> * `ABI.json`
>
> The contract must support:
>
> * `mint(address to, string memory metadataURI)`
> * `tokenURI(uint256 tokenId)`
> * `ownerOf(uint256 tokenId)`
>
> You can also implement resale functionality optionally.
>
> ---
>
> ## **📦 Deliverables**
>
> Replit must produce:
>
> * Full backend (FastAPI)
> * Full frontend (TS + React/Next.js)
> * Smart contract integration
> * AI verification service
> * QIE SDK integration for wallet + minting
> * IPFS integration
> * MongoDB models
> * End-to-end working NFT ticketing system
>
> **Generate all necessary code files, components, services, and configs automatically.**
>
> ---
>
> **End of Prompt — build the entire project.**
>
> ---

---

# ⭐ Want more?

I can also generate:

✅ Full `blockchain.py` using QIE SDK
✅ Full FastAPI backend code
✅ Full Next.js TypeScript frontend
✅ Architecture diagram
✅ Smart contract JSON

Just tell me:

**“generate backend”**
or
**“generate frontend”**
or
**“generate qie integration”**
